"""Relay — a modular FastAPI monolith (RFC-001).

The importable package is ``relay``. Modules live under ``relay.modules.*`` and share
``relay.core.*``; cross-module boundaries are enforced by import-linter (.importlinter).
"""

__version__ = "0.0.0"
